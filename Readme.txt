#Things to do for KoreBot before starting

# At Server Side
- Input the start/enddate (sdate,edate) dates in create.py (To generate the data for uploading into table)
- Run load-data.sh to load the data to database
- Start the API provider 'python3.6 query-api.py&'
- Trigger the cron job entry (edit)
- Check with localhost if it is working
-	Get current, Add Delay, Get current


#########################################################################
# AT Bot Side
#########################################################################
- Change the config global variable to new IP address
